# Written by deploy-headplane.sh at 0600 and holding a headscale API key plus
# the cookie signing secret. Never committed, and never printed by that script
# either — the file is the only copy that exists.
headplane.env
data/
config.rendered.yaml
